当前 AOT 状态：

Entity metadata: source-generated
Entity materializer: source-generated
DTO/result materializer: source-generated
Insert / InsertMany / Upsert / COPY value access: source-generated getter
IncludeMany key extraction: source-generated getter
Main NuGet package: includes source generator analyzer
Sample project-reference path: generator wired in
剩余边界不是热路径未完成，而是有意保留的 fallback/dynamic boundary：

未注册类型仍会 fallback 到 reflection materializer/accessor。
ExpressionValueReader 仍用受限反射读取 captured locals / closure fields / constants，用于 SQL 参数化；它不使用 Expression.Compile() 或动态代码。
NativeAOT publish 之前仍被当前 SDK/TFM 的 NETSDK1207 阻断，这不是当前代码路径失败。
另外，Perigon.PostgreSQL.Analyzers 项目仍是占位。它不是当前 AOT 执行路径的一部分，但作为最终交付质量门，下一块可以做 analyzer 诊断，比如未启用 source generator、未映射属性、unsupported LINQ、无 where update/delete、动态 JSON POCO 等。